// Shared configuration and default settings
const DEFAULT_SETTINGS = {
    bin1: '559888039',
    bin2: '552461',
    binMode: 'primary',
    currentBin: 'bin1',
    generateKey: 'X',
    clearKey: 'C',
    submitKey: 'H',
    cardIndex: 0,
    cardDetails: [],
    extensionEnabled: true
};

const BIN_MODE_LABELS = {
    'primary': 'Primary Only',
    'secondary': 'Secondary Only',
    'alternate': 'Auto Alternate'
};
